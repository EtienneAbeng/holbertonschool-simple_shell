#include <stdio.h>
#include <stdlib.h>

/**
 * main - Print all command line arguments
 *
 * @av: Array of strings print the commands line arguments
 *
 * Return: Always 0.
 */

int main(int ac, char **av)
{
	int i = 0;

	(void)ac;/* Cancel the argument didn't use */

	while (av[i] != NULL)
    	{
		printf("%s\n", av[i]);
		i++;
    	}
	return 0;
}
